# Timekeeping

CLI tool for logging and tracking work hours.

## Tech Stack

- Node.js, Commander.js
- Firebase / Firebase Admin SDK for persistence

## Commands

- `npm install` — install dependencies
- `npm i -g` — install globally
- `timekeep --help` — view available options
